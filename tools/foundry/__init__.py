"""Phantom Foundry — pipeline tools for reviving typefaces from scanned specimens.

Stages: fetch → cut → cast → sort → matrix → proof. Each stage reads and writes
plain files inside a face directory (see agent_docs/architecture.md).
"""

__version__ = "0.1.0"
